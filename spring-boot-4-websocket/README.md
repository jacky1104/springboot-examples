# Spring Boot 4 WebSocket (Gradle)

Simple WebSocket echo example using Spring Boot 4.0.2.

## Run

```bash
./gradlew bootRun
```

## Try it

Use a WebSocket client to send a message to `ws://localhost:8083/ws/echo` and expect `echo:<payload>`.

## Test

```bash
./gradlew test
```
